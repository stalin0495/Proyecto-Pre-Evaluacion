server:
  port: 8080
  servlet:
    context-path: /customer-services/api
spring:
  application:
    name: customer-services
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

logging:
  level:
    com.pichincha.customer: DEBUG
    org.springframework.security: DEBUG